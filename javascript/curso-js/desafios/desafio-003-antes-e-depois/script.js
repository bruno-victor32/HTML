let num = Number(window.prompt('Digite um número inteiro qualquer'))


function calcular() {
    sucessor = num + 1
    antecessor = num - 1
    window.alert  (`Depois de ${num}, temos o número ${sucessor}.\n Antes de ${num}, temos o número ${antecessor}.`)

} 
